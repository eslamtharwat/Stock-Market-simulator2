#pragma once
#include <iostream>
#include <string>
#include <sstream>
#include <conio.h>
using namespace std;
string getpassword();
string hidepassword(string password);
string showpassword(string ascii);